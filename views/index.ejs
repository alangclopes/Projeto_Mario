<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
  </head>
  <body>
    <h1>Bem-vindo ao Sistema de Votação</h1>
    <% if (!user) { %>
    <a href="/login">Login</a>
    <a href="/register">Register</a>
    <% } else { %>
    <a href="/logout">Logout</a>
    <% if (user.role === 'admin') { %>
    <a href="/candidatos">Gerenciar Candidatos</a>
    <a href="/eleitores">Gerenciar Eleitores</a>
    <% } else { %>
    <a href="/votacoes">Votações</a>
    <% } %> <% } %>
  </body>
</html>
